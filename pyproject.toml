[project]
name = "tns-serv"
version = "0.1.0"
description = "Server for the Teacher Notifier System"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "flask-admin>=2.0.2",
    "flask-sqlalchemy>=3.1.1",
    "gunicorn>=23.0.0",
    "waitress>=3.0.2",
]
